package utils

import models.LibCatalog
import org.scalatest.BeforeAndAfter
import org.scalatest.flatspec.AsyncFlatSpec

class JsonUtilSpec extends AsyncFlatSpec with BeforeAndAfter {
  /*
  "JsonUtil" should "Convert a LibCatalog in a json" in {
    JsonUtil.safeLibConvertingToJson(new LibCatalog()) should equal ()
  }
   */
}
